export enum Period {
  DAILY = 'DAILY',
  WEEKLY = 'WEEKLY',
  MONTHLY = 'MONTHLY',
  YEARLY = 'YEARLY',
}

export enum ScanLabType {
  SCAN = 'SCAN',
  LAB = 'LAB',
}

export enum Action {
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE',
  READ = 'READ',
  LOGIN = 'LOGIN',
  LOGOUT = 'LOGOUT',
}

export enum RecordType {
  DIAGNOSIS = 'DIAGNOSIS',
  VISIT_SUMMARY = 'VISIT_SUMMARY'
}
